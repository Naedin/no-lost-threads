# A slice

## Claim ledger

- fallback writers are exactly two, read off a truncated listing — `rg -n '\.fallback' src` — `a.swift:1`, `:2`
- every writer is in a — `rg -n '\.fallback' src` → `a` × 2, `b.swift`
