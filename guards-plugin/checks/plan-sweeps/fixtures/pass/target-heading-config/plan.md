# A slice

## Exit conditions

- `rg -n '\.fallback' src` → 0 hits when the slice lands.

## Acceptance

- Not a target heading under this config, so the count is compared: `rg -n '\.fallback' src` → 2 hits.
