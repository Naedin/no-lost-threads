# Process-review deferral ledger

Header prose, free.

## Live watches — each carries its promoting signal

- **Relink-engine extension** (deferred 2026-08-12). Two hand rules for the reference
  graph exist. **Signal:** a third hand rule, or a pure repoint chore for a dangle
  reachable at mutation time. **Retiring signal:** three quiet windows after 2026-10-01.
  last checked: 2026-09-06 — no fire; the delete-motion stub is unbuilt.
- **Budget state** — regenerate, never cite: `scripts/lint-docs.sh` with the report flag.
  **Signal:** an off-charter cluster forms in the largest doc.
  last checked: 2026-09-05 — three OVER, all re-baselined at measured.

## Falsifications — kept whole so no window re-chases them

- **Proxy narrower than the class** (2026-08-31). The trigger counted files where the
  class was fixes; kept whole because the next proposer will reach for the same proxy.
  The full argument, with the three cases, stays here because it is the falsification.

## Resolved — pointer at the landing commit; narrative lives there

- Relink-engine delete motion → stub filed `7e700d9f` (2026-09-05).
- Retro-log append destination → `a12ae434` (2026-09-06), hook stub retired.
  The 09-02b watch saw three quiet windows, 09-04, 09-05 and 09-06, after which
  the hook stub was retired because the decision-moment text sufficed, and the
  full reasoning was recorded across two review runs.
